# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

#config
config.json
*.gitignore
.gitignore
.windsurfrules

# Dependency directories (remove the comment below to include it)
# vendor/
/.idea/.gitignore
/.idea/eru.iml
/.idea/modules.xml
/.idea/vcs.xml
/eru-files/app
/eru-files/setEnvVar.sh
/eru-rules/setEnvVar.sh
/eru-sample/
/eru-functions/func_profile.log
.vscode/launch.json
.vscode/tasks.json

# windsurf rules
.windsurfrules
.gitignore
eru-ql/eru-ql.log
.gitignore
eru-sql-llm-analysis.md
*.md
eru-ai/app
eru-gateway/app
eru-functions/app
launch.json
tasks.json
.gitignore
eru-ai/localhost-key.pem
eru-ai/localhost.pem
/home/alty/linuxcode/goworkspace/eru/eru-ai/tools/ckyc/input
# Fixtures captured from a live tenant - real entity schemas, real pages, real
# business descriptions. They are worth keeping for regression comparison and
# are not ours to publish, so they live in one place per module and never leave
# the machine that recorded them. Committed fixtures are synthetic and sit in
# the ordinary per-package testdata/ directories.
local-testdata/
eru-ai/tools/utility/testdata
