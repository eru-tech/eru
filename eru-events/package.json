{
  "name": "kafka-ui-test",
  "version": "1.0.0",
  "description": "Playwright test for Kafka UI verification",
  "scripts": {
    "test": "playwright test",
    "test-ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}